services:
  web:
    container_name: podcast-app-web
    image: ${DOCKER_IMAGE:-podcast-app}
    build:
      context: .
      target: "service"
    ports:
      - 127.0.0.1:${APP_PORT}:${APP_PORT}
    env_file:
      - .env
    environment:
      APP_SERVICE: app
      DB_HOST: db
      REDIS_HOST: redis
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  rq:
    image: ${DOCKER_IMAGE_NAME:-podcast-service}:${DOCKER_IMAGE_TAG:-latest}
    container_name: podcast-app-rq
    build:
      context: .
      target: "service"
    profiles:
      - service
    env_file:
      - .env
    environment:
      APP_SERVICE: rq
      APP_VERSION: ${DOCKER_IMAGE_TAG:-latest}
      REDIS_HOST: redis
    networks:
      - internal-subnet
    depends_on:
      redis:
        condition: service_healthy
      web:
        condition: service_started

  db:
    container_name: podcast-app-db
    image: postgres:17-alpine
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      PGDATA: /var/lib/postgresql/data/pgdata
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER}"]
      interval: 5s
      timeout: 2s
      retries: 5
    volumes:
      - ${PWD}/.data/postgres:/var/lib/postgresql/data/pgdata

  redis:
    container_name: podcast-app-redis
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 2s
      retries: 5
    volumes:
      - ${PWD}/.data/redis:/data
    networks:
      - default
      - internal-subnet

  test:
    container_name: podcast-app-test
    image: podcast-app:test
    env_file:
      - .env
    build:
      context: .
      target: tests
      args:
        DEV_DEPS: true
    environment:
      APP_SERVICE: test
      DB_HOST: db
      REDIS_HOST: redis
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  lint:
    container_name: podcast-app-lint
    image: podcast-app:test
    env_file:
      - .env
    build:
      context: .
      target: tests
      args:
        DEV_DEPS: true
    environment:
      APP_SERVICE: lint

networks:
  internal-subnet:
